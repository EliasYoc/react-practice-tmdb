import styled from "styled-components";

export const HomeMovies = styled.section`
  display: flex;
  overflow-x: scroll;
  gap: 1rem;
  padding: 1rem;
  scroll-snap-type: x mandatory;
  scroll-padding: 1rem;
`